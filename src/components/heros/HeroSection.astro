---
import Container from '../Container.astro';

const { reversed } = Astro.props;
---

<section class="page-hero">
	<Container>
		<div class={`page-hero__grid ${reversed ? 'page-hero__grid--reversed' : ''}`}>
			<div class="page-hero__text">
				<slot name="text-container" />
			</div>
			<div class="page-hero__media">
				<slot name="image-container" />
			</div>
		</div>
	</Container>
</section>

<style>
	.page-hero {
		padding: clamp(2.5rem, 6vw, 5rem) 0 var(--gap-section);
	}

	.page-hero__grid {
		display: grid;
		grid-template-columns: 6fr 5fr;
		gap: clamp(2rem, 6vw, 5rem);
		align-items: center;
	}

	.page-hero__grid--reversed {
		grid-template-columns: 5fr 6fr;
	}

	.page-hero__grid--reversed .page-hero__text {
		order: 2;
	}

	.page-hero__text :global(h1) {
		font-size: var(--step-5);
		font-weight: 500;
		margin: 0 0 1rem;
		line-height: 1.02;
	}

	.page-hero__text :global(h1)::after {
		content: '';
		display: block;
		width: 2.5rem;
		height: 3px;
		background: var(--clay);
		margin-top: 1.25rem;
	}

	.page-hero__text :global(p) {
		font-size: var(--step-1);
		color: var(--ink-muted);
		line-height: 1.55;
		max-width: 38ch;
		margin: 0 0 1rem;
	}

	.page-hero__media :global(img) {
		width: 100%;
		height: auto;
		aspect-ratio: 4 / 5;
		max-height: 36rem;
		object-fit: cover;
		border-radius: var(--radius-md);
		display: block;
	}

	@media (max-width: 960px) {
		.page-hero__grid,
		.page-hero__grid--reversed {
			grid-template-columns: 1fr;
			gap: 2rem;
		}

		.page-hero__grid--reversed .page-hero__text {
			order: 1;
		}

		.page-hero__media :global(img) {
			aspect-ratio: 5 / 4;
		}
	}
</style>
